{% extends 'base.html.twig' %}

{% block title %}Editar producto | Clientess{% endblock %}

{% block body %}
    <div class="page-header">
        <span class="text-primary fw-semibold">Productos</span>
        <h1 class="h2 mb-1">Editar producto</h1>
        <p class="text-secondary mb-0">Actualiza los datos del producto.</p>
    </div>
    <div class="card form-card shadow-sm">
        <div class="card-body">{{ include('producto/_form.html.twig', {'button_label': 'Guardar cambios'}) }}</div>
    </div>
    <div class="mt-3">{{ include('producto/_delete_form.html.twig') }}</div>
{% endblock %}
